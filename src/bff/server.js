import { authorize, register, logout, fetchCategorys, fetchBestsellers, fetchProducts } from './operations';

export const server = {
	authorize,
	register,
	logout,
	fetchCategorys,
	fetchBestsellers,
	fetchProducts,
};
